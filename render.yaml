services:
  - type: web
    name: gaming-wellness-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT
    envVars:
      - key: RIOT_API_KEY
        sync: false
      - key: FIREBASE_CREDENTIALS_JSON
        sync: false
